diff --git a/node_modules/y-prosemirror/src/plugins/sync-plugin.js b/node_modules/y-prosemirror/src/plugins/sync-plugin.js
index dccfb76..8acb05a 100644
--- a/node_modules/y-prosemirror/src/plugins/sync-plugin.js
+++ b/node_modules/y-prosemirror/src/plugins/sync-plugin.js
@@ -108,6 +108,7 @@ export const ySyncPlugin = (yXmlFragment, {
   mapping
 } = {}) => {
   let initialContentChanged = false
+  let previousSyncedDoc = null
   const binding = new ProsemirrorBinding(yXmlFragment, mapping)
   const plugin = new Plugin({
     props: {
@@ -196,6 +197,16 @@ export const ySyncPlugin = (yXmlFragment, {
       onFirstRender()
       return {
         update: () => {
+          if (view.state.doc === previousSyncedDoc) {
+            // Nothing about the document changed since the last update —
+            // this call was triggered by a metadata-only transaction
+            // (e.g. yCursorPlugin's deferred awareness-driven setMeta).
+            // Resyncing anyway would still fire a real Y transaction with
+            // the tracked ySyncPluginKey origin below, which clears
+            // yUndoPlugin's redo stack even though nothing changed.
+            return
+          }
+          previousSyncedDoc = view.state.doc
           const pluginState = plugin.getState(view.state)
           if (
             pluginState.snapshot == null && pluginState.prevSnapshot == null
diff --git a/node_modules/y-prosemirror/src/plugins/undo-plugin.js b/node_modules/y-prosemirror/src/plugins/undo-plugin.js
index 7c9448b..2facf14 100644
--- a/node_modules/y-prosemirror/src/plugins/undo-plugin.js
+++ b/node_modules/y-prosemirror/src/plugins/undo-plugin.js
@@ -107,7 +107,17 @@ export const yUndoPlugin = ({ protectedNodes = defaultProtectedNodes, trackedOri
     undoManager.on('stack-item-added', ({ stackItem }) => {
       const binding = ystate.binding
       if (binding) {
-        stackItem.meta.set(binding, yUndoPluginKey.getState(view.state).prevSel)
+        // binding.beforeTransactionSelection (set by beforeAllTransactions,
+        // captured before this item's Y-level mutation ran) is preferred
+        // over the plugin state's own `prevSel` field: for a stack item
+        // added while undoing/redoing, prevSel gets computed by the
+        // mirrored PM transaction's own apply() *after* the Y mapping has
+        // already been pruned of the content that was just deleted, which
+        // degrades to a doc-start fallback instead of the real position —
+        // producing a redo that always lands the cursor at the start of
+        // the document instead of where the restored content ends.
+        const sel = binding.beforeTransactionSelection || yUndoPluginKey.getState(view.state).prevSel
+        stackItem.meta.set(binding, sel)
       }
     })
     undoManager.on('stack-item-popped', ({ stackItem }) => {
